[user]
        email = joey.parsons@gmail.com
        name = joeyparsons
[url "git@github.com:"]
        insteadOf = https://github.com/
[color]
        ui = auto
[color "branch"]
        current = yellow reverse
        local = yellow
        remote = green
[color "status"]
        added = yellow
        changed = green
        untracked = cyan
[core]
        whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
        editor = vim
        quotepath = false
[alias]
        st = status -sb
        ci = commit
        all = commit -am
        br = branch
        co = checkout
        di = diff
        lg = log -p
        lol = log --graph --decorate --pretty=oneline --abbrev-commit
        lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
        ls = ls-files
[http "https://pkg.re"]
        followRedirects = true
[diff "sopsdiffer"]
        textconv = sops -d

